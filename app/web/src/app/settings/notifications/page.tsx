import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Notification settings" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Notification settings" description="Choose which updates you receive." />;
}
