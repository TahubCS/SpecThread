import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Create your first project" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Create your first project" description="Set up a project and choose the team that owns it." />;
}
