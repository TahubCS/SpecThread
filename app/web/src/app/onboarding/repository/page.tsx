import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Connect a repository" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Connect a repository" description="Connect the GitHub repository that supplies implementation evidence." />;
}
