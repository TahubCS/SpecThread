import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Create a team" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Create a team" description="Create a group that can own projects." />;
}
