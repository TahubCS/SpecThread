import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Project overview" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Project overview" description="See requirements, repository status, reviews, and recent activity." />;
}
