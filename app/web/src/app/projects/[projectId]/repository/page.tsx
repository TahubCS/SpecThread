import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Repository overview" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Repository overview" description="See the connected GitHub repository and sync status." />;
}
