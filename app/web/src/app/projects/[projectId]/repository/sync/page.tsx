import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Repository sync" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Repository sync" description="Inspect sync status and failures." />;
}
