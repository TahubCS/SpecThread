import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Verification run" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Verification run" description="Inspect one recorded verification run." />;
}
