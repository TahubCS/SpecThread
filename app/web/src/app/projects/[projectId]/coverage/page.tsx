import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Evidence coverage" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Evidence coverage" description="Find requirements with missing evidence or checks." />;
}
