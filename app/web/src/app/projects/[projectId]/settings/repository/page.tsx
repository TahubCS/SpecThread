import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Repository settings" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Repository settings" description="Connect or manage this project's GitHub repository." />;
}
