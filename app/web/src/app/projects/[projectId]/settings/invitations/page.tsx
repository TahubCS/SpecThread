import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Project invitations" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Project invitations" description="Manage direct project invitations, if supported." />;
}
