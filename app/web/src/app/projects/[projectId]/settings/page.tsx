import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Project settings" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Project settings" description="Manage project details and archive state." />;
}
