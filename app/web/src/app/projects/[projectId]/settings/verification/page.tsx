import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Verification settings" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Verification settings" description="Configure the project's predefined verification scenario." />;
}
