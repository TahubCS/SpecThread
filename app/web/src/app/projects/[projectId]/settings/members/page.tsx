import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Project member settings" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Project member settings" description="Manage project-specific access, if supported." />;
}
