import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Saved views" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Saved views" description="Open saved project filters." />;
}
