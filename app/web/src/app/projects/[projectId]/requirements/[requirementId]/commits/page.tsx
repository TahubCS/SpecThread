import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Commits" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Commits" description="Inspect commits linked to this requirement." />;
}
