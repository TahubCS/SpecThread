import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Suggestions" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Suggestions" description="Review suggested links or summaries before accepting them." />;
}
