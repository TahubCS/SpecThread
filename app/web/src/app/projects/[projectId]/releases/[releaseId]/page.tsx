import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Release details" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Release details" description="Inspect evidence associated with one release." />;
}
