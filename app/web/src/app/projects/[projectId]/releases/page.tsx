import { ScaffoldPage } from "@/components/scaffold-page";

/** Renders the "Releases" placeholder with scaffold navigation and no connected product data or actions. */
export default function Page() {
  return <ScaffoldPage title="Releases" description="See release and deployment references linked to this project." />;
}
